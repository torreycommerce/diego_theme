{% extends "/_layouts/base.html.twig" %}
{% block content %}

{# Widget Carousel or Default Carousel #}
{% include "/_partials/carousel.html.twig" %}

{% include "/_partials/product_grid/index.html.twig" %}

{# Promo Sale Widget #}
{% set promo_sale = api.get('widget/render', {'slug' : 'promo-sale'}) %}
{% if promo_sale is not empty %}
  {{ promo_sale }}
{% endif %}

{# Brand Carousel Widget #}
{% set brandCarousel = api.get('widget/render', {'slug' : 'brand-carousel'}) %}
{% if brandCarousel is not empty %}
 {{ brandCarousel }}
{% endif %}

{# Newsletter Widget #}
{%- import "/_macros/form-elements-bootstrap.html.twig" as newsletterform -%}
{% set newsletterBox = api.get('widget/render', {'slug' : 'newsletter'}) %}
{% if newsletterBox is not empty %}
  {{ newsletterBox }}
{% endif %}

{% endblock %}
